---
trigger:
  batch: true
  branches:
    include:
      - master

pr: none

pool:
  vmImage: "ubuntu-latest"

variables:
  - group: github

stages:
  - stage: "Docs"
    displayName: "Docs"
    condition: eq(variables['Build.SourceBranch'], 'refs/heads/master')
    jobs:
      - job: "Docs_MkDocs"
        displayName: "MkDocs"
        condition: eq(variables['Build.SourceBranch'], 'refs/heads/master')
        steps:
          - script: |
              sudo apt install -y git git-fast-import openssh-client
              pip install mkdocs mkdocs-material pymdown-extensions
            displayName: "Install"
          - script: |
              python --version
              pip --version
              python -m mkdocs gh-deploy -v -c -m "📚 Update docs"
            displayName: "MkDocs"
