const styles = theme => ({
  grid: {
    height: "100%",
    width: "fit-content",
    paddingLeft: theme.spacing.unit * 2,
    paddingRight: theme.spacing.unit * 2,
    flexWrap: "nowrap",
    overflowY: "hidden"
  }
});

export default styles;
